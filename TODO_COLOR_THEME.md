# TODO: Color Theme Upgrade (Neon Noir Teal)

- [x] Update `src/app/globals.css` with new accent variables (teal/cyan) + scrollbar/selection/body background gradients.

- [x] Update `GlobalBackground.tsx` vignette/smoke/grain colors from crimson → teal.

- [ ] Update `FloatingNav.tsx` border/underline/button CTA from crimson → teal.
- [ ] Update `CrimsonCursor.tsx` reticle dot/ring colors from crimson → teal (file name can stay).
- [ ] Update `Hero.tsx` glow/CTA/stroke accents from crimson → teal.
- [ ] Update `Filmography.tsx` card borders/glow/labels from crimson → teal.
- [ ] Update `Quotes.tsx` marquee + borders/shadows from crimson → teal.
- [ ] Update `src/app/page.tsx` top banner strip accent from crimson → teal.
- [ ] Update `FilmReelSpindle.tsx` ambient haze, projector beams, core/plasma, hover overlays, modal accents from crimson/red → teal.
- [ ] Run `npm run lint` and `npm run build`.

